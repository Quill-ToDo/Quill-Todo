name: Coverage
on: pull_request
jobs:
  generateCoverage:
    name: Generate coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - run: cd ./tasks-fe && jest --ci --coverage && codecov
  uploadCoverage:
    name: Upload coverage to Codecov
    needs: generateCoverage
    steps:
      - uses: codecov/codecov-action@v2